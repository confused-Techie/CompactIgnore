{
  "name": "compactignore",
  "version": "1.0.0",
  "description": "Method of managing multiple Ignore and Config Files Easily.",
  "main": "index.js",
  "bin": "./bin/compactignore.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "prettier --check -u -w ."
  },
  "author": "confused-Techie",
  "license": "GPL-3.0",
  "devDependencies": {
    "prettier": "^2.5.1"
  }
}
